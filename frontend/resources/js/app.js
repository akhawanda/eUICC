// Livewire 3 ships its own Alpine bundle and auto-starts it. Do not import
// or start Alpine here — duplicating it triggers "Detected multiple
// instances of Alpine running" and breaks every wire:click + @-event.
import './bootstrap';
